import { Router } from 'express';

const router = Router();

// Placeholder for sync routes
router.post('/', (req, res) => res.json({ message: 'Sync routes not yet implemented.' }));

export default router;