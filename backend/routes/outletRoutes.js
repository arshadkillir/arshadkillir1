import { Router } from 'express';

const router = Router();

// Placeholder for outlet routes
router.get('/', (req, res) => res.json({ message: 'Outlet routes not yet implemented.' }));

export default router;