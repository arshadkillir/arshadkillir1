export const getMenu = async (req, res) => {
  return res.json({ message: 'getMenu placeholder' });
};

export const uploadMenu = async (req, res) => {
  return res.json({ message: 'uploadMenu placeholder' });
};
