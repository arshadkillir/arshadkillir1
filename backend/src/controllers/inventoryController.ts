export const getInventory = (req, res) => {
  return res.json({ message: 'getInventory OK' });
};

export const searchInventory = (req, res) => {
  return res.json({ message: 'searchInventory OK' });
};

export const adjustStock = (req, res) => {
  return res.json({ message: 'adjustStock OK' });
};
